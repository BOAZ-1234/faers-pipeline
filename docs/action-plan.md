# 액션플랜 — FAERS 신호탐지 파이프라인

*기획서 8장(일정·역할)·5-1(조인키)·12장(리스크·열린결정)을 실행 체크리스트로 재구성한 것.
새 태스크를 지어내지 않았다 — 기획서에 이미 결정된 것만 순서대로 나열했다.
사람이 채워야 할 빈칸(★)은 기획서에 없는 것이라 임의로 채우지 않았다.*

---

## 0. 착수 전 — 사람이 지금 정해야 하는 것

이 4개는 기획서 어디에도 실명·날짜가 없다. 아래 체크리스트를 시작하려면 먼저 정해야 한다.

- [x] ★ **A/C/B/D 단계 리드 실명 배정** — A: 이지훈, B: 이수연, C: 함하경, D: 장수연
- [ ] ★ **프로젝트 시작일(Week 1 기준일) 확정**
- [ ] ★ **라벨 분류 감사 200건**(12장) — 담당자와 시기. 기획서엔 "남아있다"고만 적혀 있고 누가 언제 할지는 없음
- [ ] ★ **정답지 시간 공백**(2018·2019년 FDA 보고서 없음) 재확인 — 담당자 미정

---

## 0-1. 이번 주(2026-08-25 주) 액션 — 담당자별

A단계가 2020~2026년 적재 완료, 1~2일 내 전량 적재 마무리 예상 시점에 짠 주간 계획.

**팀 공통 — 최우선**
- [x] **조인키 표준 회의**(7개 키 확정) — `ingredient_norm` 염접미사 목록은 제 초안(build_map.py의 SALT_SUFFIXES 27개) 그대로 채택하기로 합의
- [ ] 회의 결과를 **dbt contract로 강제** + 조인율 품질 테이블 세팅 — 담당 미정, 이번 주 안에 정해야 함
- [ ] **openFDA API 키 발급** — 함하경(C) 몫으로. bulk 다운로드(C세부1이 씀)는 키 불필요, 건별 검색 API(reaction_dict PR#1이 이미 씀, C세부2도 곧 씀)에 필요
- [ ] CI 뼈대(GitHub Actions) — **이번 주는 보류, 나중으로**

**이지훈 (A)**
- [ ] 전체 백필 마무리 (1~2일 예상)
- [ ] **A세부3 몫 — 고유 약물명 추출 + Drugs@FDA 시점 라벨 수집**
- [ ] **완료 후 → C세부2(2단계 유사도 매칭) 합류** — C리드(함하경)가 캐스케이드 설계·세부3·API키까지 이미 맡고 있어 세부2까지 얹으면 임계경로가 한 사람에 몰림. 로테이션 원칙(§8·1-1)대로 A 끝나는 대로 이지훈이 세부2 담당

**이수연 (C세부1, B리드 예정)**
- [ ] 커버리지 1차 측정 (A 고유 약물명 리스트 나오는 대로)
- [ ] PR#8(drug_ingredient_map) 리뷰받고 머지
- [ ] 식약처 제품명 개행문자 163건 정리

**함하경 (C리드 + C세부3)**
- [ ] C세부3 3단계 마무리 — LLM 판정 + 316개 전수 확인
- [ ] PR#1(부작용 사전) 리뷰받고 머지
- [ ] **C리드 몫 — 캐스케이드 설계(1단계 사전→2단계 유사도→3단계 LLM 연결) + Langfuse 세팅**

**장수연 (D세부2)**
- [ ] 정답지 1,715쌍 채점셋/봉인예측셋/학습셋 분할
- [ ] 최근 구간(2023년 이후) 컷오프 로직
- [ ] PR#7(채점 엔진) 리뷰받고 머지

---

## 1. Week 1 — 전원 참여, A단계 리드 주재

기획서가 명시적으로 "1주차에 고정"이라고 못 박은 것들. 이걸 놓치면 "뒤 단계가 앞 단계를 기다리다 3개월을 날린다"(5-1).

- [x] **조인키 표준 회의** — 5-1의 7개 키를 그 자리에서 확정 (완료, 2026-08-25 주)
  - [x] `ingredient_norm` (대문자·공백단일화·염/수화물접미사 제거) — 접미사 목록 27개(build_map.py `SALT_SUFFIXES`) 그대로 채택
  - [x] `ingredient_set` (정렬한 성분 소문자, `|` 연결)
  - [x] `unii` (FDA 물질코드 10자)
  - [x] `reaction_pt` (FAERS 원형 그대로)
  - [x] `pt_set_id` (정렬한 용어 소문자, `|` 연결 — 하나짜리도 묶음으로)
  - [x] `as_of` (분기 말일)
  - [x] `snapshot_id` (Iceberg 스냅샷 ID)
- [ ] 위 7개를 **dbt contract로 강제** + 조인율을 품질 테이블에 적재하는 구조 세팅 — 담당 미정
- [ ] **openFDA API 키 발급** (12장 열린결정 — 후보 100개까지는 키 없이 되지만 1,000개부터 필요) — 담당 미정
- [ ] **C단계 사전 두 개(`drug_ingredient_map`, `signal_term→pt_set_id`) 스키마만 먼저 확정** — `drug_ingredient_map` 스키마는 오늘 C세부1 산출물로 사실상 확정됨(medicinalproduct/ingredient_norm/unii/method/confidence). `signal_term→pt_set_id`는 C세부3 쪽 미확인
- [ ] **CI 뼈대(GitHub Actions)**: lint + 단위 테스트 + 기준 파일 비교 — 담당 미정

---

## 1-1. A단계와 겹쳐서 시작 가능한 것 — 대기 없이 바로 착수

5장 "C의 입력은 8,400만 행이 아니라 고유 약물명이라 A 직후 바로 착수할 수 있다"와
8장 "고유 약물명은 전량 백필을 기다리지 않는다"에 따라, A가 다 끝나야 C·D가 시작되는 구조가 아니다.
아래 3개는 **A의 결과물 자체가 필요 없어서 Week 1부터 바로 시작 가능** — A를 진행하는 인원과
겹치지 않게 배분하면 아무도 놀지 않는다.

- [ ] **C세부1 — 사전 4종 통합.** FAERS와 무관한 별도 공개 데이터라 A 진행과 무관 (이슈#5·PR#8, 전량 완료·커버리지만 대기)
  - [x] openFDA 라벨 API에서 브랜드명↔성분명 쌍 추출 (262,032건 전량)
  - [x] openFDA NDC에서 제품코드↔성분명 쌍 추출 (137,198건 전량)
  - [x] RxNorm에서 브랜드명↔성분명 추출 (5,123개 브랜드 전량) — 단, "+동의어" 부분은 실시간 매칭 API(`approximateTerm.json`)라 정적 추출 불가로 판명돼 **C세부2 2단계 유사도 매칭으로 이관**
  - [x] 식약처 허가에서 한글 제품명↔영문 성분명 추출 (5-2 국내연결 화면에도 재사용) (42,960건 전량)
  - [x] 4개를 표기 규칙 하나로 통일해 병합 → `drug_ingredient_map` 1차본 (195,787개 고유 쌍)
  - [ ] 이 사전만으로 잡히는 비율(커버리지) 1차 측정 — GPU 산정의 전제 조건 (A세부3 고유 약물명 리스트 대기 중, 블록됨)
- [ ] **C세부3 — 부작용 사전 316개.** FDA 정답지 원문은 3장 기준 이미 스크랩 완료 상태라 FAERS 적재를 기다릴 이유 없음 (이슈#6·PR#1)
  - [x] 0단계: 316개 중 부작용이 아닌 것(라벨링·투약오류·기기) 30개(9%) 제외
  - [x] 1단계: 영국식 철자·어순 규칙 정규화 (표본 300 중 155쌍이 여기서 해결됨)
  - [x] 2단계: 안 풀린 것은 openFDA 검색으로 후보 100개+건수 확보
  - [ ] 3단계: LLM 판정 + **316개 전수 사람 확인** (생략 시 조용히 오염되는 위험 — Hearing/Renal impairment 사례)
- [ ] **D세부2 — 채점기 골격.** "랜덤 순위표로 먼저 완성할 수 있다"고 문서에 명시돼 있어 완전히 독립적 (이슈#4·PR#7)
  - [ ] FDA 정답지 1,715쌍을 채점셋 1,071쌍(양성857·음성214)/봉인예측셋 573쌍/학습셋 1,142건으로 분할
  - [x] 완전 일치 매칭 로직 구현
  - [x] Recall@K(K=100·500·1000·5000) 계산 함수
  - [x] Lift(랜덤 대비 배수) 계산 함수
  - [x] 검토대상 축소율 계산 함수
  - [ ] 최근 구간(2023년 이후) 컷오프 로직
  - [x] 순서를 무작위로 섞은 가짜 순위표로 전체 스모크 테스트

> 이렇게 앞당겨 시작한 인원도 C·D단계가 정식 착수되면 세부1/2/3 로테이션에 다시 합류한다 —
> 8장 "전원참여" 원칙은 "모든 단계를 동시에 시작하지 말라"가 아니라 "한 사람이 6개월 내내 한 컴포넌트만
> 파지 말라"는 뜻이라 이 앞당기기와 충돌하지 않는다.

---

## 2. A단계 — 원본 적재 (1개월, 리드: 이지훈)

**게이트**: 전량 적재 + 고유 약물명 리스트

- [ ] 리드 — 파티션·멱등성 설계 (신고ID+버전 기준 merge, 백필·증분 몇 번 돌려도 결과 동일)
- [ ] 리드 — Airflow 도입 여부 판단 (배치가 분기 1회라 스케줄러로는 과함 → 이번 단계에 써보고 판단, 채택 근거는 "의존성 관리"로)
- [ ] 세부1 — 분기 zip 백필 파이프라인 → Iceberg
- [ ] 세부2 — 증분 적재 + 실패 격리 테이블(버려지지 않고 별도 테이블) + 품질 지표 적재
- [ ] 세부3 — **고유 약물명 추출** — ★ 전량 백필을 기다리지 말고 1개 분기분이라도 먼저 C단계로 넘길 것
- [ ] 세부3 — Drugs@FDA 시점 라벨 수집 착수 (D단계 필터 공급원, 확보율 77.5% 예상)

---

## 3. C단계 — 이름 통일 (2개월, 리드: 함하경) ⚠ 임계경로

**게이트**: 정확도 + 커버리지 개선폭 (Recall@K 아님)

> C단계가 8,400만 행 전체 파이프라인의 병목이다. 2개월로 가장 길고, 늦어지면 B·D가 통째로 밀린다.
> 아래 세 장치가 그 방어책이다 — 순서대로 적용된다.

- [ ] **방어장치 1: 1주차 스키마 고정** — 위 0·1번 항목으로 이미 커버됨
- [ ] 리드 — 캐스케이드 설계(1단계 사전조회 → 2단계 유사도 → 3단계 임베딩+LLM)
- [ ] 리드 — Langfuse 세팅 (LLM 호출 단위 추적)
- [x] 세부1(이수연) — 사전 4종(openFDA 라벨·NDC·RxNorm·식약처 허가) 통합 + 1단계 정규화·조회
- [ ] 세부1(이수연) — **커버리지 1차 측정** ← 이 숫자가 나와야 GPU 산정 시작 가능 (A세부3 대기, 블록됨)
- [ ] 세부2(이지훈, A 완료 후 합류) — 2단계 유사도 매칭 구현
- [ ] 세부2(이지훈) — 골든셋 자동 생성 (FDA가 이미 채운 성분 필드 54.4%에서 필드 가리기)
- [ ] 세부2(이지훈) — 후보 개수 운영점 탐색 ((계산량, 정확도) 곡선 그려서 결정)
- [ ] **방어장치 2: 1·2단계 완료되는 대로 중간 릴리스** — 전체 2개월 끝까지 기다리지 않음
- [ ] 세부3(함하경) — 부작용 사전 316개: 0단계 분류 → 1단계 규칙 정규화 → 2단계 후보 뽑기(완료) → 3단계 LLM 판정 + **316개 전수 사람 확인**(남음)
- [ ] **방어장치 3: v0 스모크** — 세부3이 이름 통일 완료를 기다리지 않고 브랜드 키(원본 문자열)로 축소판을 끝까지 관통. 통과 조건은 정확도가 아니라 "끝까지 돌고 채점 숫자가 나오는가". 여기서 만든 기준 파일이 그대로 B단계 CI 픽스처가 됨
- [ ] 🔲 **GPU 산정** — 세부1 커버리지 측정 나온 뒤에만 진행 (그 전엔 조달 판단 안 함)

---

## 4. B단계 — 펼치고 집계 (1.5개월, 리드: 이수연)

**게이트**: 순위표 v1 완주

- [ ] 리드 — 4칸 집계 + IC025 구현
- [ ] 리드 — 묶음 그레인(성분 묶음×부작용 묶음)으로 뺄셈 유도 다시 하기 — v0(브랜드 단위)와 그레인이 다르므로 재작업 필요
- [ ] 세부1 — 8,400만 행 펼치기 + 성분 조인
- [ ] 세부1 — 쏠림 처리(salting) — 아스피린처럼 신고가 몰리는 약물 대응
- [ ] 세부2 — 시점 델타 누적(분기 늘어난 양만 저장)
- [ ] 세부2 — 전량 재집계 대조 (분기 1회, 누적과 안 맞으면 원본정정 or 버그)
- [ ] 세부3 — 기준 파일 회귀 구축
- [ ] 세부3 — CI 픽스처 (소형 픽스처 1~2 파티션을 레포에 커밋)
- [ ] 세부3 — 구조 검사 ("집계에 들어간 신고 최대 날짜 ≤ T" — 미래 정보 누수 탐지)

---

## 5. D단계 — 순위와 평가 (1.5개월, 리드: 장수연)

**게이트**: Recall@K · Lift · 리드타임 확정

- [ ] 리드 — 랭킹 가중치 학습 (shrinkage logistic regression, 학습셋 1,142건)
- [ ] 리드 — MLflow 세팅 (params에 `snapshot_id` 필수 포함)
- [ ] 리드 — v1(IC025 단독) → v2(+시점라벨필터) → v3(+중대성) → v4(+끊음/재투여) → v5~ 순차 운영
- [ ] 세부1 — 게이트 구현 (`IC025 > 0` 그리고 `a ≥ 3`, 고정값)
- [ ] 세부1 — 필터 구현 (그 시점 라벨에 이미 있으면 후보 제거)
- [ ] 세부1 — **동의어 묶음 21개 포함** — 안 넣으면 라벨 발견율 60%에 그쳐 필터가 절반 샘
- [ ] 세부2(장수연) — 채점기(Recall@100/500/1000/5000·Lift·검토대상 축소율) — ★ 랜덤 순위표로 먼저 완성 가능, 파이프라인 출력 나오면 입력만 교체 (골격 완료, 실데이터 대기)
- [ ] 세부2(장수연) — 전체 백테스트
- [ ] 세부3 — dbt 마트 (contract로 조인키 강제)
- [ ] 세부3 — FastAPI (`GET /snapshot?as_of=...&ingredient=...`)
- [ ] 세부3 — 화면 2개(① 시점 슬라이더 순위표 ② 국내 연결 조회)
