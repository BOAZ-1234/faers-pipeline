# 착수 전 feasibility 실험 전부 (스크립트 · 산출물 · 원본데이터 · 개인 기획문서)
_probe/

# 가상환경 / 캐시 / IDE
.venv/
venv/
__pycache__/
*.py[cod]
.vscode/
.idea/
.DS_Store
.claude/settings.local.json

# 비밀값
.env
.env.*
*.pem
*.key

# 데이터 · 산출물
data/
out/
*.parquet
*.csv
*.db
*.sqlite3
*.duckdb
*.duckdb.wal
bronze/verification/*.json

# 새 폴더 구조로 정리 중 — 기존 폴더 전부 임시 ignore
samples/

#브론즈 데이터 폴더
bronze/data

# 정답지 gold — 감사본 label(2,268행, 사람+LLM 정정, 재생성 불가)만 예외로 추적.
# 채점 재현성용. info_full 등 스크랩 원문은 제외했고 나머지 labelset/data/ 는 계속 무시.
!labelset/data/
labelset/data/*
!labelset/data/labelset_gold.csv


